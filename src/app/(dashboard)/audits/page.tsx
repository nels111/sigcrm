import { AuditsPageClient } from "@/components/audits/audits-page-client";

export const metadata = {
  title: "Audits | Signature Cleans CRM",
};

export default function AuditsPage() {
  return <AuditsPageClient />;
}
