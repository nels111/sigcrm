import { IssuesPageClient } from "@/components/issues/issues-page-client";

export const metadata = {
  title: "Issues & Complaints | Signature Cleans CRM",
};

export default function IssuesPage() {
  return <IssuesPageClient />;
}
