import { IssueDetailClient } from "@/components/issues/issue-detail-client";

export const metadata = {
  title: "Issue Detail | Signature Cleans CRM",
};

export default function IssueDetailPage() {
  return <IssueDetailClient />;
}
