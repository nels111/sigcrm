import { CalendarPageClient } from "@/components/calendar/calendar-page-client";

export const metadata = {
  title: "Calendar | Signature Cleans CRM",
};

export default function CalendarPage() {
  return <CalendarPageClient />;
}
