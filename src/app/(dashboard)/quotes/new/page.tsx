import { QuoteForm } from "@/components/quotes/quote-form";

export const metadata = {
  title: "New Quote | Signature Cleans CRM",
};

export default function NewQuotePage() {
  return <QuoteForm />;
}
