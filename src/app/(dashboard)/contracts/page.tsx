import { ContractsPageClient } from "@/components/contracts/contracts-page-client";

export const metadata = {
  title: "Contracts | Signature Cleans CRM",
};

export default function ContractsPage() {
  return <ContractsPageClient />;
}
