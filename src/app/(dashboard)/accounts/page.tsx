import { AccountsPageClient } from "@/components/accounts/accounts-page-client";

export const metadata = {
  title: "Accounts | Signature Cleans CRM",
};

export default function AccountsPage() {
  return <AccountsPageClient />;
}
