import { DocumentsPageClient } from "@/components/documents/documents-page-client";

export const metadata = {
  title: "Documents | Signature Cleans CRM",
};

export default function DocumentsPage() {
  return <DocumentsPageClient />;
}
