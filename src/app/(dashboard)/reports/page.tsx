import { ReportsPageClient } from "@/components/reports/reports-page-client";

export const metadata = {
  title: "Reports | Signature Cleans CRM",
};

export default function ReportsPage() {
  return <ReportsPageClient />;
}
