import { MarketingPageClient } from "@/components/marketing/marketing-page-client";

export const metadata = {
  title: "Marketing | Signature Cleans CRM",
};

export default function MarketingPage() {
  return <MarketingPageClient />;
}
