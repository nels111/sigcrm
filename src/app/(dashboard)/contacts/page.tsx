import { ContactsPageClient } from "@/components/contacts/contacts-page-client";

export const metadata = {
  title: "Contacts | Signature Cleans CRM",
};

export default function ContactsPage() {
  return <ContactsPageClient />;
}
