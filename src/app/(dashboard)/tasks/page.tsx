import { TasksPageClient } from "@/components/tasks/tasks-page-client";

export const metadata = {
  title: "Tasks | Signature Cleans CRM",
};

export default function TasksPage() {
  return <TasksPageClient />;
}
