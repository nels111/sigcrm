import { LeadsPageClient } from "@/components/leads/leads-page-client";

export const metadata = {
  title: "Leads | Signature Cleans CRM",
};

export default function LeadsPage() {
  return <LeadsPageClient />;
}
